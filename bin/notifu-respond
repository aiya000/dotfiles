#!/bin/bash

# (github) copilot-cliが/pディレクトリと/mディレクトリの読み書き権限を求めてくるので、ワークアラウンド
#（/pと/mはディレクトリではない。オプション。）

your_name=$1
message=$2
if [[ $message == '' ]] ; then
  message="$your_name respond"
fi

( notify "$your_name" "$message" ) &
