# .gemini_global

Here is template settings for gemini-cli.

## Setup

```shell-session
$ [[ -d ~/.gemini ]] || mkdir ~/.gemini

$ cp ~/.dotfiles/.gemini_global/settings.json ~/.gemini/settings.json
$ $EDITOR ~/.gemini/settings.json  # ../bash-toys maybe necessary
```
