# .copilot

Here is template settings for Claude Code.

## Setup

```shell-session
$ [[ -d ~/.copilot ]] || mkdir ~/.copilot
$ ln -s ~/.dotfiles/.copilot/copilot-instructions.md ~/.copilot/copilot-instructions.md
```
